spring:
  jpa:
    defer-datasource-initialization: false
    hibernate:
        ddl-auto: create-drop

jpa:
  entities:
    base-package: com.ilan.entity

query:
  dsl:
    sql:
      package:
        directory: beta.querydsl.sql
