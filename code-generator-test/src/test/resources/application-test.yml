spring:
  jpa:
    defer-datasource-initialization: false
    hibernate:
        ddl-auto: create-drop

jpa:
  entities:
    base-package: com.ilan.entity

sql:
  queryDsl:
    output:
      directory: generated-test-sources
    package:
      directory: com.querydsl.sql
